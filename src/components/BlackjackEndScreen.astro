<div id="end-screen">
    <h1>Game Over</h1>
    <div class="results">
      <h2 id="result-message"></h2>
      <h3 id="main-bet-result"></h3>
    </div>
    <button id="continue-btn">Continue</button>
  </div>
  
  <style>
    /* End screen container */
    #end-screen {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background-color: #333;
      color: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.25);
      text-align: center;
      width: 400px;
      display: none;
    }
  
    /* Title styling */
    #end-screen h1 {
      font-size: 32px;
      margin-bottom: 20px;
    }
  
    /* Results section */
    .results h2 {
      font-size: 20px;
      margin-bottom: 5px;
    }
  
    .results p {
      font-size: 18px;
      margin-bottom: 15px;
      font-weight: bold;
    }
  
    /* Winnings section */
    .winnings {
      margin-top: 20px;
      font-size: 18px;
    }
  
    .winnings-item {
      display: flex;
      justify-content: space-between;
      margin-bottom: 10px;
      font-size: 16px;
    }
  
    .winnings-item.total {
      font-weight: bold;
      font-size: 20px;
    }

    #continue-btn {
      display: block;
      margin: 0 auto;   /* centers it horizontally */
      padding: 10px 20px;
      font-size: 16px;
      font-weight: bold;
      border: 2px solid #007BFF;
      border-radius: 5px;
      background-color: #007BFF;
      color: white;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.2s;
      width: fit-content;  /* so it only wraps text instead of full width */
    }

  
    /* Responsive design */
    @media (max-width: 500px) {
      .end-screen {
        width: 90%;
      }
    }
  </style>
  